import React from 'react';
import './Hero.css';

const Hero = () => {
  return (
    <div className="hero">
      <h1 className="hero-title">Holy places and pilgrimage sites are where the individual with an external quest receives what he comes for, and the individual with an internal quest rises high from within.</h1>
      <p className="hero-subtitle">Discover the most revered religious sites around the world.</p>
      <button className="hero-button">Explore Now</button>
    </div>
  );
};

export default Hero;
